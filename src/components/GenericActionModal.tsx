import React from "react";

/**
 * GenericActionModal has been completely removed from the system per Enterprise HIS guidelines.
 * All workflows are executed directly within their specific module contexts (In-place Workflows).
 */
export default function GenericActionModal() {
  return null;
}
